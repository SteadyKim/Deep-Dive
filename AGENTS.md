# AGENTS.md

## 목적

이 저장소는 공부한 내용을 정리하는 개인 지식 저장소다.  
사용자가 공부하고 싶은 주제를 입력하면, 기존 저장소의 구조와 스타일을 참고해 새로운 Markdown 문서를 생성한다.

## 기본 동작 흐름

사용자가 특정 주제에 대한 학습 자료 생성을 요청하면, 아래 순서를 반드시 따른다.

1. 먼저 저장소의 폴더 구조와 기존 Markdown 파일들을 확인한다.
2. 주제와 연관된 폴더와 문서들을 탐색한다.
3. 아래 항목을 파악한다.
    - 가장 적절한 상위 폴더
    - 파일명 규칙
    - 문서 내부 구조
    - 해당 영역에서 주로 쓰는 말투와 깊이
4. 특별한 요청이 없으면 `.md` 파일은 정확히 1개만 생성한다.
5. 새 문서는 반드시 Markdown 형식으로 작성하고, 인접한 문서들의 형식과 최대한 일관되게 맞춘다.
6. 파일 생성 후에는 적절한 커밋 메시지를 함께 제안한다.
7. `git push` 전에는 반드시 사용자에게 확인을 받는다.
8. 사용자의 명시적 승인 없이는 push하지 않는다.

## 파일 위치 결정 규칙

파일 위치는 아래 규칙을 위에서부터 순서대로 적용해 결정한다.

1. 주제와 명확히 연관된 기존 폴더가 있으면 그 폴더에 넣는다.
2. 적절한 폴더가 없으면, 주제에 맞는 짧고 명확한 이름의 새 폴더를 만든다.
3. 어떤 폴더 안에 유사한 주제의 파일이 이미 5개 이상 있으면, depth를 한 단계 늘린 하위 폴더를 새로 만들고 그 안에 넣는다.
4. 관련 주제는 최대한 한곳에 모은다.
5. 굳이 새 최상위 폴더를 만들기보다, 기존의 가까운 도메인 폴더에 넣는 편이 더 자연스러우면 그 구조를 우선한다.

## 유사 주제 판단 규칙

아래 중 하나 이상 해당하면 유사 주제로 본다.

- 같은 기술 도메인
- 같은 서브도메인
- 같은 학습 트랙
- 같은 알고리즘 / 자료구조 계열
- 같은 인프라 / 플랫폼 카테고리
- 같은 데이터베이스 / 내부 동작 카테고리

단순 파일명 유사도만 보지 말고, 주변 폴더의 목적과 문서 맥락까지 함께 고려한다.

## 파일 생성 규칙

새 Markdown 파일을 만들 때는 아래 원칙을 따른다.

1. 가능하면 같은 폴더 안의 기존 문서 구조를 재사용한다.
2. 아래 항목은 주변 문서와 최대한 맞춘다.
    - 제목 스타일
    - 헤더 구조
    - bullet 사용 방식
    - 예시 작성 방식
    - 코드 블록 사용 여부
    - 요약 섹션 구성
3. 문서는 단순 설명문이 아니라 실제로 다시 읽기 좋은 학습 자료여야 한다.
4. 가능하면 구체적인 설명, 예시, 트레이드오프, 실무 감각을 포함한다.
5. 사용자가 명시적으로 수정 요청한 것이 아니라면 기존 파일을 덮어쓰지 않는다.
6. 내용은 다른 파일과 비교해 중복을 최소화해야한다.

## 파일명 규칙

파일명을 정할 때는 아래 원칙을 따른다.

1. 대상 폴더에서 이미 사용 중인 파일명 스타일을 따른다.
2. 짧되, 주제가 분명하게 드러나야 한다.
3. 나중에 git 히스토리에서 파일명만 봐도 내용을 짐작할 수 있어야 한다.
4. `study.md`, `note.md`, `temp.md` 같은 모호한 이름은 피한다.

## 문서 내용 규칙

새로 만드는 Markdown 문서는 상황에 따라 보통 아래 내용을 포함한다.

- 주제 개요
- 핵심 개념
- 동작 원리
- 중요한 트레이드오프
- 실전 예시
- 요약

단, 대상 폴더의 기존 문서들이 다른 구조를 사용하고 있다면 그 로컬 규칙을 우선한다.

## 커밋 메시지 규칙

파일 생성 후에는 커밋 메시지를 생성한다.  

커밋 메시지는 아래를 반영해야 한다.

- 주제

## Push 안전 규칙

기본적으로 push는 하지 않는다.  
원격 저장소에 push하는 모든 작업 전에는 반드시 사용자에게 확인을 받는다.

응답에는 아래 내용을 포함한다.

- 파일 생성 완료 여부
- 제안하는 커밋 메시지
- commit / push 진행 여부에 대한 사용자 확인 요청

## 구조가 애매할 때의 판단 원칙

저장소 구조가 다소 애매하면 아래 우선순위대로 판단한다.

1. 가장 가까운 관련 문서와의 일관성 우선
2. 새 폴더 남발보다 기존 구조 활용 우선
3. 일반적인 베스트 프랙티스보다 저장소 내부 관례 우선
4. 합리적인 선택을 한 뒤, 응답에서 그 이유를 짧게 설명

## 작업 범위 제한

명시적 요청이 없으면 아래 작업은 하지 않는다.

- 관련 없는 파일 리팩토링
- 기존 폴더명 변경
- 저장소 전체 구조 재정리
- 여러 기존 문서 동시 수정
- 주제와 무관한 보일러플레이트 추가

## 완료 조건

아래 조건을 모두 만족해야 작업이 완료된 것으로 본다.

- 새 Markdown 파일이 적절한 위치에 생성되었다
- 파일 구조가 주변 문서 규칙과 일관된다
- 내용이 자연스럽고 실용적이다
- 커밋 메시지가 준비되었다
- push 전 사용자 확인 요청이 포함되었다
